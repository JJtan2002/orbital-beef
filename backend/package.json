{
    "name": "cashflow",
    "version": "1.0.0",
    "scripts": {
        "build": "python3 -m pip install -r requirements.txt && python3 manage.py makemigrations && python3 manage.py migrate --output dist"
    }
}